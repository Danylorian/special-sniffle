import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.caissefamiliale.app',
  appName: 'Caisse Familiale',
  webDir: 'dist'
};

export default config;
